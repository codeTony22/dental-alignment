{
  "name": "viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The demo's 3D viewer stack, copied (plan §3/AM-5, copy-debt ledger row 3): sceneController + Viewer3D, verifyScene + VerifyViewer, and their pure geometry/colour/routing rules with their tests. Consumed ONLY by apps/product; the frozen apps/web keeps its own originals.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "18.3.1",
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/react": "18.3.31",
    "@types/three": "0.185.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
